<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
</head>
<body>
  <h1>Edit Post</h1>
  <form action="/blogposts/<%= blogpost._id %>?_method=PUT" method="POST">
    <div>
      <label for="title">Title</label>
      <input type="text" id="title" name="blogpost[title]" value="<%= blogpost.title %>">
    </div>
    <div>
      <label for="body">Body</label>
      <input type="text" id="body" name="blogpost[body]" value="<%= blogpost.body %>">
    </div>
    <div>
      <button>Update Post</button>
    </div>
  </form>
  <a href="/blogposts/<%= blogpost._id %>">Back to Post</a>
</body>
</html>
